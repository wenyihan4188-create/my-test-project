# Utility Tools

Small daily-use tools that do not need their own top-level category yet.

Included tools:

- Image Compressor
- Directory Tree Generator

Generated text outputs are written to `outputs/` when needed. Image compression
runs in the browser and downloads directly.
